<h1>All Places</h1>


  <div class="container">
    <div class="row">

      <% @places.each do |place| %>
      <div class='all_places'>
        <%= link_to place.city, place_path(place) %> •
      </div>
      <% end %>
  </div>
</div>


  <!-- <div class="row"> -->

    <!-- <% @places.in_groups_of(5) do |place1, place2| %>
    <div class='row'>
      <div class="col-xs-6">
        <%= link_to place1.city, place_path(place1) %>
        <% if place2 %>
        <div class="col-xs-6">
        <%= link_to place2.city, place_path(place2) %>
      </div>
        <% end %>
      </div>
    </div>

    <% end %> -->

<!-- </div> -->

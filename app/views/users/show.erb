<h1><%= @user.full_name %></h1>


<h2>Places I've Been To!</h2>
<% @user.visited_places.each do |place| %>
<ul>
  <li>
    <%= link_to "#{place.city} - #{place.country}", place_path(place) %>
  </li>
</ul>
<% end %>

<h2>Places I will Go To Someday</h2>
<% @user.will_go_to_someday.each do |place| %>
<ul>
  <li>
    <%= link_to "#{place.city} - #{place.country}", place_path(place) %>
  </li>
</ul>
<% end %>

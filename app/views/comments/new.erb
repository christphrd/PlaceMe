<%= form_for @comment do |f| %>
  <%= f.label "Your Text Here!" %>
  <%= f.text_area :content %>
  <%= f.hidden_field(:place_id, value: @place) %>
  <%= f.submit "Add Comment", :class => "btn btn-primary" %>
<% end %>
